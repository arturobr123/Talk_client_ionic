import { ImageViewerComponent } from './image-viewer.component';
import { PanGesture } from 'ionic-angular/gestures/drag-gesture';
import { DomController } from 'ionic-angular/util/dom-controller';
export declare class ImageViewerSliderGesture extends PanGesture {
    private component;
    private translationX;
    private startX;
    private initialTranslation;
    private image;
    private firstTimestamp;
    private windowWidth;
    constructor(component: ImageViewerComponent, domCtrl: DomController);
    onDragStart(ev: any): boolean;
    canStart(): boolean;
    onDragMove(ev: any): boolean;
    onDragEnd(ev: any): boolean;
}
