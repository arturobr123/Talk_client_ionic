import { CSS } from 'ionic-angular/util/dom';
var getRegExp = function (direction) { return new RegExp(".*translate" + direction + "((-?[0-9]+)(px)?)", 'i'); };
export var getTranslationValues = function (element) {
    var regex = new RegExp(".*translate((-?[0-9]+)(px)?,(-?[0-9]+)(px)?)", 'i');
    var style = element.style[CSS.transform];
    var xMatch = (getRegExp('X').exec(style) || []).map(function (v) { return parseInt(v, 10); });
    var yMatch = (getRegExp('Y').exec(style) || []).map(function (v) { return parseInt(v, 10); });
    return {
        x: xMatch[1] || 0,
        y: yMatch[1] || 0
    };
};
//# sourceMappingURL=translation-helper.js.map