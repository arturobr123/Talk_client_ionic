import { App } from 'ionic-angular';
import { Component, ContentChildren, Input, forwardRef } from '@angular/core';
import { ImageViewerDirective } from './image-viewer.directive';
import { ImageViewer } from './image-viewer';
export var ImageViewerCarouselComponent = (function () {
    function ImageViewerCarouselComponent(_app) {
        this._app = _app;
    }
    ImageViewerCarouselComponent.prototype.onClick = function (item) {
        var clickedItemPosition = item.getNativeElement().getBoundingClientRect();
        var images = this.imageViewerItems.map(function (i) { return i.getNativeElement().src; });
        var imageViewer = ImageViewer.create({ images: images, position: clickedItemPosition });
        this._app.present(imageViewer, {});
    };
    ImageViewerCarouselComponent.decorators = [
        { type: Component, args: [{
                    selector: 'image-viewer-carousel',
                    template: "\n\t\t<div [ngClass]=\"cssClass\">\n\t\t\t<ng-content></ng-content>\n\t\t</div>\n\t"
                },] },
    ];
    /** @nocollapse */
    ImageViewerCarouselComponent.ctorParameters = [
        { type: App, },
    ];
    ImageViewerCarouselComponent.propDecorators = {
        'cssClass': [{ type: Input },],
        'imageViewerItems': [{ type: ContentChildren, args: [forwardRef(function () { return ImageViewerDirective; }),] },],
    };
    return ImageViewerCarouselComponent;
}());
//# sourceMappingURL=image-viewer-carousel.component.js.map